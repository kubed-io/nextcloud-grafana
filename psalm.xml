<?xml version="1.0"?>
<!--
	SPDX-FileCopyrightText: 2026 Kelly Ferrone
	SPDX-License-Identifier: AGPL-3.0-or-later
-->
<psalm
	errorLevel="6"
	resolveFromConfigFile="true"
	findUnusedBaselineEntry="false"
	findUnusedCode="false"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns="https://getpsalm.org/schema/config"
	xsi:schemaLocation="https://getpsalm.org/schema/config vendor/vimeo/psalm/config.xsd"
	errorBaseline="tests/psalm-baseline.xml">
	<projectFiles>
		<directory name="lib" />
		<ignoreFiles>
			<directory name="vendor" />
		</ignoreFiles>
	</projectFiles>
	<!--
		Load the nextcloud/ocp class definitions so Psalm resolves OCP\* symbols
		(IConfig, Files\Node, AppFramework\Controller, the Settings interfaces, …).
		Without this, Psalm reports every OCP reference as UndefinedClass /
		MissingDependency / UndefinedDocblockClass — ~176 false positives. We scan
		only the OCP package (not all of vendor) to keep analysis fast.
	-->
	<extraFiles>
		<directory name="vendor/nextcloud/ocp" />
	</extraFiles>
	<!--
		The admin-connection POC references only classes shipped in nextcloud/ocp
		(the App framework, Settings interfaces, IClientService, ICrypto, IAppConfig,
		Console command bases). It touches none of the private OC internals, Sabre/DAV
		types, or other-app event classes the master's sync engine did — so there are
		no issue-handler suppressions to carry here. They return with the code that
		needs them in the sync chapters.
	-->
</psalm>
