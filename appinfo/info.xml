<?xml version="1.0"?>
<!--
  - SPDX-FileCopyrightText: 2026 kubed-io
  - SPDX-License-Identifier: AGPL-3.0-or-later
-->
<info xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:noNamespaceSchemaLocation="https://apps.nextcloud.com/schema/apps/info.xsd">
  <id>grafana_sync</id>
  <name>Grafana Sync</name>
  <summary>Your Grafana dashboards as native, editable, backed-up Nextcloud files</summary>
  <description><![CDATA[
Grafana Sync turns a Nextcloud folder into a live mirror of a Grafana folder.

Map a Grafana **folder** to a Nextcloud folder and everything inside it shows up: subfolders become folders, dashboards become real `.grafana` files with the Grafana icon. Click one and the live dashboard opens in Grafana.

Nothing is matched on filename. Every file carries its dashboard's uid, so renaming, moving, copying, trashing and restoring never break the link — and re-running a sync never duplicates anything.

## ✨ What you can do

- 📊 **Edit a dashboard as a file** — save it in the Files app, over WebDAV or from the desktop client, and Grafana has it
- 🗂️ **Keep the whole tree** — Grafana has real nested folders, and both sides mirror each other at any depth
- 🚚 **Move** a dashboard between folders or between mappings — same dashboard, same uid, same URL
- 🍝 **Copy** one, and get a real new dashboard in Grafana
- 🗑️ **Trash, restore and purge**, with an optional Grafana recycle bin that preserves ids
- 🏷️ **Tag** a dashboard on either side — the file, the JSON and Grafana all carry one set
- ➕ **Create** a dashboard from the Files app's **+ New** menu
- 🖱️ **Open in Grafana**, or open the JSON in a text editor
- ⌨️ **Script all of it** — every admin action has an `occ` command

## 🗂️ Real folders, all the way down

Grafana has real, nested folders, so there is no tagging scheme to maintain and no flat namespace to fake. The mapping is a plain folder-to-folder mirror and the whole tree comes with it, in both directions.

Move a folder and every dashboard under it is re-parented in one gesture, uids intact — not deleted and re-created to get there. Rename it on either side and the other follows, because the link is the folder's uid rather than its name. A folder holding no dashboards stays an ordinary folder, however much else is in it.

## 🗑️ A recycle bin Grafana never had

Grafana has no trash. Delete a dashboard there and it is gone; delete a folder and it takes everything under it, in one request, with nothing to undo it.

So this app adds one. Name any ordinary Grafana folder as the recycle bin, and trashing a dashboard file **parks** its dashboard there instead of deleting it — id, URL and history kept. Restore the file and it comes straight back out. Only emptying the Nextcloud trash makes it permanent.

That turns an irreversible cascade into a move. It works from the Grafana side too: purge the parked dashboards there and the trashed file follows them; drag them back out and the trashed folder returns, uids intact.

## 🧭 Sync or Link — the folder decides

| Mode | The file holds | Pushes back? |
|---|---|---|
| **Sync** | The full dashboard JSON | Yes — fully bidirectional |
| **Link** | A pointer (uid, title, URL) | No — clicking it opens Grafana |

**Link** is for dashboards somebody else owns — provisioned by an operator, managed in Git, generated by a chart. Nextcloud treats them as read-only and a DAV guard stops anything overwriting the pointer.

A **sync** mapping is therefore also a real backup: every dashboard in the folder, complete and versioned by Nextcloud.

## 🎨 Honest icons, honest dates

Dashboards register their own mimetype, so they wear the real Grafana icon instead of a generic braces glyph. And each file carries **the dashboard's** created and modified dates rather than the sync's — so Recent, sorting and search all tell you the truth about your work.

📖 **[Full documentation and the `occ` reference on GitHub](https://github.com/kubed-io/nextcloud-grafana#readme)**

Requires a Grafana service-account token with access to the folders you map. This app is in early development: it works end to end and is covered by tests against a real Grafana, but has not yet been run widely.

---

This is a community integration and is not affiliated with, endorsed by, or sponsored by Grafana Labs. "Grafana" and the Grafana logo are trademarks of Grafana Labs, used here only to identify the service this app integrates with.
  ]]></description>
  <version>0.1.0</version>
  <licence>agpl</licence>
  <author>kubed-io</author>
  <namespace>GrafanaSync</namespace>
  <category>integration</category>
  <category>files</category>
  <!--
    DECLARED SO THE APP IS LOADED ON WEBDAV REQUESTS. Without this the
    trash-purge hook is dead code, and it is invisible until you look for it.

    `register()` runs for every enabled app during bootstrap, but `boot()` only runs
    when an app is actually LOADED — and remote.php, the WebDAV entry point, loads a
    restricted set:

        $appManager->loadApps(['authentication']);
        $appManager->loadApps(['extended_authentication']);
        $appManager->loadApps(['filesystem', 'logging']);

    With no <types>, this app was never in that set, so boot() never ran on a DAV
    request. Everything wired in register() — the copy, move, rename, create,
    soft-delete and restore listeners — kept working, which is exactly what made the
    gap so hard to see: only the ONE thing wired in boot(), the legacy trash-purge
    hook ({@see OCA\GrafanaSync\Listener\TrashPurgeHook}), was silently dead. A
    perfectly correct hook, connected in a method that never ran.

    The cost here is specific: with the recycle bin ON, emptying the Nextcloud trash
    is the step that permanently deletes the parked dashboard from the Grafana bin
    folder. A dead purge hook means those dashboards accumulate in the bin forever —
    the admin empties their trash and Grafana quietly keeps every "deleted" dashboard.

    `filesystem` is the type for apps that must be present when the filesystem is in
    play, which is what a file-event-driven mirror is. Found in the n8n sibling first
    (saga §5.7), where the same hook was dead for the same reason.
  -->
  <types>
    <filesystem/>
  </types>
  <documentation>
    <user>https://github.com/kubed-io/nextcloud-grafana#readme</user>
    <admin>https://github.com/kubed-io/nextcloud-grafana#-setup-in-three-moves</admin>
    <developer>https://github.com/kubed-io/nextcloud-grafana/blob/main/CONTRIBUTING.md</developer>
  </documentation>
  <website>https://github.com/kubed-io/nextcloud-grafana</website>
  <bugs>https://github.com/kubed-io/nextcloud-grafana/issues</bugs>
  <repository type="git">https://github.com/kubed-io/nextcloud-grafana.git</repository>
  <dependencies>
    <!-- The FLOOR IS 31 BY CAPABILITY AND 32 BY SUPPORT, and it declares 32.
         AutoSyncSettings implements IDeclarativeSettingsFormWithHandlers (@since
         31.0.0), which is the only way an ADMIN declarative form can carry a CHECKBOX
         at all — core's INTERNAL storage throws a TypeError on both the read and the
         write of a bool (see that class's docblock). This app has two, and one of them
         (bin_enabled) decides whether a delete is recoverable. 31 is therefore the
         technical minimum; the declared floor is 32 because 30 and 31 are both past
         end of life, so nothing supported is lost by asking for the newer one. -->
    <nextcloud min-version="32" max-version="34"/>
  </dependencies>
  <settings>
    <!-- Section order (by priority): Instance (5, base URL + token in one card —
         Grafana has one API + one credential, so no split) → Sync Settings (20,
         the scheduled pull) → Recycle Bin (25, what a delete means) → Folder
         mappings (30) → Sync Actions (45, all the buttons). The first three are
         declarative forms registered in AppInfo\Application::register(). AdminTest
         is NOT rendered as its own panel — it stays only as the
         #[AuthorizedAdminSetting] target for the test endpoint; the
         Test-connection button lives in the Sync Actions panel. -->
    <admin>OCA\GrafanaSync\Settings\MappingSettings</admin>
    <admin>OCA\GrafanaSync\Settings\SyncSettings</admin>
    <admin-section>OCA\GrafanaSync\Settings\AdminSection</admin-section>
  </settings>
  <repair-steps>
    <!--
      Installs the application/grafana+json mimetype + icon mapping on every
      install/upgrade. Idempotent; see lib/Migration/RegisterMimetype.php.
    -->
    <install>
      <step>OCA\GrafanaSync\Migration\RegisterMimetype</step>
    </install>
    <post-migration>
      <step>OCA\GrafanaSync\Migration\RegisterMimetype</step>
    </post-migration>
    <uninstall>
      <!-- Mirror image of RegisterMimetype: reverts the core-tree mimetype/icon
           registration so removing the app leaves Nextcloud clean (store rule).
           Touches only the system registration — never user files/metadata/Grafana.
           See lib/Migration/UnregisterMimetype.php. -->
      <step>OCA\GrafanaSync\Migration\UnregisterMimetype</step>
    </uninstall>
  </repair-steps>
  <commands>
    <command>OCA\GrafanaSync\Command\SetToken</command>
    <command>OCA\GrafanaSync\Command\TestConnection</command>
    <command>OCA\GrafanaSync\Command\AddMapping</command>
    <command>OCA\GrafanaSync\Command\ListMappings</command>
    <command>OCA\GrafanaSync\Command\RemoveMapping</command>
    <command>OCA\GrafanaSync\Command\SetGroups</command>
    <command>OCA\GrafanaSync\Command\Reconcile</command>
  </commands>
</info>
